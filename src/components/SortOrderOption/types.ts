// TODO: add current level option to this? Or possibly could be separate
export type AssignmentSortOption =
  | "shuffled"
  | "srs stage, asc"
  | "srs stage, desc"
  | "level, asc"
  | "level, desc"
  | "available date, asc"
  | "available date, desc";

// TODO: change to this data structure
// type Option = "shuffled" | "srs stage" | "level" | "available date";

// type Order = "asc" | "desc" | "neither";

// export type AssignmentSortOption = { option: Option; order: Order };
