export type SwitchSize = "small" | "medium" | "large";

export type SwitchSizeMapValue = {
  rootSize: {
    width: string;
    height: string;
  };
  toggleSize: {
    width: string;
    height: string;
  };
};
